import XCTest
@testable import swiftui_mapview

final class swiftui_mapviewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftui_mapview().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
